test inheritance.HighscoreList
	
	instance sub = new HighscoreList(2), TestListener l1 = new TestListener(sub), TestListener l2 = new TestListener(sub)
		
	
	sequence addItem "Tester Œ legge til nye elementer i listen" {
		"Legger til 1. resultat" -- sub.addItem(5) -->
		state {
			sub.list.get(0) == 5
		}
		"Legger til 2. resultat" -- sub.addItem(3) -->
		state {
			sub.list.get(0) == 3
			sub.list.get(1) == 5
		}
		"Legger til bedre resultat" -- sub.addItem(2) -->
		state {
			sub.list.get(0) == 2
			sub.list.get(1) == 3
		}
	}
	
	sequence listListeners "Tester Œ legge til, fjerne og oppdatere observat¿rer" {
		"Legge til ListListener og oppdater liste" -- sub.addListListener(l1), sub.addListListener(l2), sub.addItem(3) -->
		state {
			l1.list.get(0) == 3
			l2.list.get(0) == 3
		}
		"Fjerne listListener og sjekke lister" -- sub.removeListListener(l1), sub.addItem(1) -->
		state {
			l1.list.get(0) == 3
			l1.list.size() == 1
			l2.list.get(0) == 1
			l2.list.size() == 2
		}
	}
    